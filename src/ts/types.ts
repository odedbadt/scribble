export type Vector2 = {
    x: number;
    y: number;
}
export type Rect = {
    x: number;
    y: number;
    w: number;
    h: number;
}

export const unit_rect:Rect = {x:0,y:0,w:1,h:1}
export const zero_vector:Vector2 = {x:0,y:0}

